package es.pedrazamiguez.expenseshareapp.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// --- Refined Ocean Voyage Palette ---
// Inspired by sophisticated travel/finance apps with a modern Material 3 Expressive feel

// Primary: Deep Teal (Trust, Finance, Stability)
// More saturated and vibrant than before
val OceanTeal = Color(0xFF00796B)           // Richer teal
val OceanTealLight = Color(0xFFB2DFDB)      // Soft mint for containers (not baby blue)
val OceanTealDark = Color(0xFF80CBC4)       // Softer dark mode primary

// Secondary: Warm Slate (Professional, grounded)
// Warmer undertones for a more inviting feel
val SlateBlue = Color(0xFF546E7A)           // Blue-grey with warmth
val SlateBlueContainer = Color(0xFFCFD8DC)  // Light grey-blue container
val SlateBlueDark = Color(0xFFB0BEC5)       // Soft slate for dark mode

// Tertiary: Warm Coral/Amber (Action, Expenses, Energy)
// Less radioactive, more sophisticated warmth
val AdventureCoral = Color(0xFFFF7043)      // Warm coral-orange
val AdventureCoralContainer = Color(0xFFFFCCBC) // Soft peach container
val AdventureCoralDark = Color(0xFFFFAB91)  // Muted coral for dark mode

// Backgrounds & Surfaces
// Cleaner whites and deeper darks
val SurfaceDay = Color(0xFFFAFAFA)          // Clean off-white
val SurfaceNight = Color(0xFF121212)        // True dark

// Surface Container variants for depth
val SurfaceContainerLowest = Color(0xFFFFFFFF)
val SurfaceContainerLow = Color(0xFFF5F5F5)
val SurfaceContainer = Color(0xFFEEEEEE)
val SurfaceContainerHigh = Color(0xFFE0E0E0)
val SurfaceContainerHighest = Color(0xFFD6D6D6)

// Dark Surface Container variants
val SurfaceContainerLowestDark = Color(0xFF0D0D0D)
val SurfaceContainerLowDark = Color(0xFF1A1A1A)
val SurfaceContainerDark = Color(0xFF212121)
val SurfaceContainerHighDark = Color(0xFF2C2C2C)
val SurfaceContainerHighestDark = Color(0xFF383838)

// Error
val ErrorRed = Color(0xFFD32F2F)            // Slightly softer red
val ErrorRedContainer = Color(0xFFFFCDD2)   // Soft pink container
