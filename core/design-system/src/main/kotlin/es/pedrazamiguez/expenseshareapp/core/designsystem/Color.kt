package es.pedrazamiguez.expenseshareapp.core.designsystem

import androidx.compose.ui.graphics.Color

// --- Ocean Voyage Palette ---

// Primary: Deep Teal (Trust, Finance, Stability)
val OceanTeal = Color(0xFF006874)
val OceanTealLight = Color(0xFF97F0FF) // For containers
val OceanTealDark = Color(0xFF4FD8EB) // For Dark Mode primary

// Secondary: Slate Blue/Grey (Subtle, professional)
val SlateBlue = Color(0xFF4A6267)
val SlateBlueContainer = Color(0xFFCDE7EC)
val SlateBlueDark = Color(0xFFB1CBD0)

// Tertiary: Vibrant Coral (Action, Expenses, Fun)
// Used for FABs, Call-to-Actions, and highlighting money spent
val AdventureCoral = Color(0xFFEE7F55)
val AdventureCoralContainer = Color(0xFFFFDBCF)
val AdventureCoralDark = Color(0xFFFFB59D)

// Backgrounds & Surfaces
val SurfaceDay = Color(0xFFF5FAFB) // Slightly tinted white, not stark white
val SurfaceNight = Color(0xFF0F1516) // Deep dark, almost black but tinted

// Error
val ErrorRed = Color(0xFFBA1A1A)
val ErrorRedContainer = Color(0xFFFFDAD6)
