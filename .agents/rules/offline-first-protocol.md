## Offline-First Protocol

Save to Room first for instant UI updates, generate UUIDs and timestamps locally (never let Firestore generate them), then sync to Firestore in the background using reusable sync delegates.
